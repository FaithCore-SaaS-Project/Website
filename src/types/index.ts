// ─── Shared Application Types ─────────────────────────────────────────────────
// Add shared TypeScript interfaces and types here as the app grows.
// e.g. API response shapes, shared enums, domain models, etc.
//
// Note: lucide-react ships its own built-in type definitions — no need
// to re-declare them here.

export {};
